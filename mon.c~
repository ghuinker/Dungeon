#include <stdio.h>
#include <stdint.h>
#include <string.h>

# include "mon.h"
# include "dungeon.h"

void init_monsters(dungeon_t *d, char nummon){
  printf("Creating %d monsters\n", nummon);

  mon_t *mons[nummon];

  d->nummon = nummon;
  d->monsters = 
}
